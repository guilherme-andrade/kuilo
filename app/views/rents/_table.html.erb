<div class="row" id="rents-table">
  <div class="col-2">
    <div class="bg-light rounded border p-3 h-100">
      <%= render 'rents/filters' %>
    </div>
  </div>
  <div class="col-10 pl-4">
    <div class="border-bottom font-weight-normal px-3 py-2 mb-2">
      <div class="row">
        <div class="col-2">
          <%= sort_link(@query, :code, 'Código', anchor: 'rents-table') %>
        </div>
        <div class="col-2">
          <%= sort_link(@query, :name, 'Nome', anchor: 'rents-table') %>
        </div>
        <div class="col-2">
          <%= sort_link(@query, :typology, 'Tipologia', anchor: 'rents-table') %>
        </div>
        <div class="col-2">
          <%= sort_link(@query, :status, 'Estado', anchor: 'rents-table') %>
        </div>
        <div class="col-2">
          <%= sort_link(@query, :market_value_cents, 'Valor de Mercado', anchor: 'rents-table') %>
        </div>
        <div class="col-2">
          <%= sort_link(@query, :default_rent_cents, 'Renda', anchor: 'rents-table') %>
        </div>
      </div>
    </div>

    <% if @rents.empty? %>
      <p>No rents found.</p>
    <% else %>
      <%= render partial: 'rents/table-card', collection: @rents, as: :property %>
    <% end %>

    <%= will_paginate @rents, list_classes: %w(pagination justify-content-end) %>
  </div>
</div>
</div>
