<!DOCTYPE html>
<html>
  <%= render 'head' %>

  <body class="dashboard">
    <%= render Comments::ModalComponent.new(commentable: @commentable) if @show_comments_modal %>
    <header class="dashboard-header">
      <%= render 'navbar' %>
    </header>
    <section class="dashboard-body">
      <main class="dashboard-main">
        <%= render 'flashes' %>
        <%= yield %>
      </main>
    </section>
    </div>
  </body>
</html>
